'use strict'

exports.__esModule = true
exports.getSchemaNames = getSchemaNames
exports.default = getSchemaName

var _utils = require('../utils')

function _ref(s) {
  return s.trim()
}

function getSchemaNames(knex) {
  let {searchPath} = (0, _utils.client)(knex).config

  if (searchPath !== void 0) {
    searchPath =
      typeof searchPath === 'string' ? searchPath.split(',') : searchPath
    return searchPath.map(_ref)
  }

  return ['public']
}

function getSchemaName(knex) {
  return getSchemaNames(knex)[0]
}
