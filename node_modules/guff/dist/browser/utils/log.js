import chalk from 'chalk'

function log() {
  var _console

  for (
    var _len = arguments.length, args = new Array(_len), _key = 0;
    _key < _len;
    _key++
  ) {
    args[_key] = arguments[_key]
  }

  return (_console = console).log.apply(
    _console,
    ['\u2592 ' + chalk.bold('guff')].concat(args)
  )
}

log.warn = function() {
  var _console2

  for (
    var _len2 = arguments.length, args = new Array(_len2), _key2 = 0;
    _key2 < _len2;
    _key2++
  ) {
    args[_key2] = arguments[_key2]
  }

  return (_console2 = console).warn.apply(
    _console2,
    [chalk.bgYellow.black('\u2592'), chalk.bold('guff')].concat(args)
  )
}

log.success = function() {
  var _console3

  for (
    var _len3 = arguments.length, args = new Array(_len3), _key3 = 0;
    _key3 < _len3;
    _key3++
  ) {
    args[_key3] = arguments[_key3]
  }

  return (_console3 = console).log.apply(
    _console3,
    [chalk.bgGreen.black('\u2592'), chalk.bold('guff')].concat(args)
  )
}

export default log
